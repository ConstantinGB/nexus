[module]
id = "git"
label = "Git"
description = "Manage Git repositories — GitHub, self-hosted, or local."
tags = ["dev", "vcs"]
system = false
prefix = "git"

[setup]
has_setup_screen = true
config_check = "git.type"
